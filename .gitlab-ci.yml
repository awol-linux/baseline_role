---
image: awollinux/dind-molecule:latest

stages:
    - lint
    - converge
services:
  - docker:dind

before_script:
  - ssh-keygen -f /root/.ssh/id_rsa -t rsa -N ''
  - docker info
  - python3 --version
  - ansible --version
  - molecule --version

lint:
    stage: lint
    script:
        - molecule lint
centos 8:
    stage: converge
    script:
        - molecule test -s centos8
centos 7:
    stage: converge
    script:
        - molecule test -s centos7
ubuntu 18.04:
    stage: converge
    script:
        - molecule test -s ubuntu1804
ubuntu 20.04:
    stage: converge
    script:
        - molecule test -s ubuntu2004
archlinux:
    stage: converge
    script:
        - molecule test -s archlinux
fedora 31:
    stage: converge
    script:
        - molecule test -s fedora31
fedora 32:
    stage: converge
    script:
        - molecule test -s fedora32
fedora 33:
    stage: converge
    script:
        - molecule test -s fedora33
